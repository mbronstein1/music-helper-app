import styled from 'styled-components';

const BackgroundImage = styled.img`
  position: absolute;
  top: 0;
  bottom: 0;
  z-index: -2;
  object-fit: cover;
  width: 100%;
`;

export default BackgroundImage;
